import StationPage from './StationPage.vue';

// const routes: Array<RouteRecordRaw> = [{ path: '/', component: ReadingsPage }];
const routes = [{ path: '/station/:id', component: StationPage }];

export default { routes };
