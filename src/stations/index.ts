import StationsPage from './StationsPage.vue';

// const routes: Array<RouteRecordRaw> = [{ path: '/', component: ReadingsPage }];
const routes = [{ path: '/stations', component: StationsPage }];

export default { routes };
